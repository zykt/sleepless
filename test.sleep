1 2.3

'(1 2 3)

((lambda (x) (add 1 x)) 41)

(define lst '(Hello World !))

(define f (lambda (x)
            (add x
                 (add x 1))))

(f 1000)

(if #t 'fire 'stop)

(if #f 'dead 'undead)

(define true #t)

(if true 'hello 'bye)

(define rec
    (lambda (x)
        (if x
            'end
            (rec #t))))

(rec #f)

(define factorial
  (lambda (x)
    (if (= x 1)
      1
      (mul x (f (minus x 1))))))

(factorial 2)
